# استكشاف أخطاء نظام Dark Mode

## 🔍 خطوات الاختبار السريعة

### الخطوة 1: فتح وحدة التحكم (Console)
```
اضغط: F12 أو Ctrl+Shift+I
انتقل إلى: Console
```

### الخطوة 2: تشغيل الفحص الشامل
```javascript
themeDebug.runAllChecks()
```

هذا سيعطيك تقرير شامل عن حالة النظام.

## 🛠️ أوامر التصحيح المتاحة

### فحص حالة النظام
```javascript
// فحص مدير الوضع
themeDebug.checkManager()

// فحص الزر
themeDebug.checkButton()

// فحص التخزين المحلي
themeDebug.checkStorage()

// فحص فئة dark في HTML
themeDebug.checkDarkClass()
```

### التحكم اليدوي بالوضع
```javascript
// تبديل الوضع يدويًا
themeDebug.manualToggle()

// تعيين الوضع المظلم
themeDebug.setDark()

// تعيين الوضع الفاتح
themeDebug.setLight()
```

## 🐛 المشاكل الشائعة والحلول

### المشكلة 1: الزر لا يعمل عند الضغط عليه

**الأعراض:**
- الضغط على الزر لا يغير الوضع
- لا توجد رسائل خطأ في Console

**الحل:**
```javascript
// تحقق من أن مدير الوضع محمل
themeDebug.checkManager()

// إذا كان الناتج false، جرب:
window.location.reload()

// إذا استمرت المشكلة، تحقق من:
themeDebug.checkButton()
```

### المشكلة 2: الوضع لا يتغير على الفور

**الأعراض:**
- الضغط على الزر يستغرق وقتاً
- الألوان تتغير ببطء

**الحل:**
```javascript
// تحقق من الأداء
console.time('theme-toggle');
themeDebug.manualToggle();
console.timeEnd('theme-toggle');

// إذا كان الوقت أكثر من 100ms، امسح الـ cache:
// Ctrl+Shift+Delete (Windows/Linux)
// Cmd+Shift+Delete (Mac)
```

### المشكلة 3: الاختيار لا يتم حفظه

**الأعراض:**
- عند إغلاق المتصفح وإعادة فتحه، يعود الوضع الافتراضي
- localStorage فارغ

**الحل:**
```javascript
// تحقق من localStorage
themeDebug.checkStorage()

// إذا كان فارغاً، جرب:
themeDebug.setDark()
themeDebug.checkStorage()

// إذا لم يتم الحفظ، قد تكون هناك مشكلة في المتصفح
// جرب متصفح آخر أو امسح بيانات المتصفح
```

### المشكلة 4: الألوان غير صحيحة

**الأعراض:**
- الألوان لا تتطابق مع التصميم
- بعض العناصر لا تتغير

**الحل:**
```javascript
// تحقق من فئة dark
themeDebug.checkDarkClass()

// تحقق من أن HTML يحتوي على الفئة:
document.documentElement.classList

// إذا لم تكن موجودة، جرب:
themeDebug.setDark()
```

## 📋 قائمة التحقق الشاملة

### قبل الاختبار
- [ ] تأكد من تشغيل خادم التطوير (`npm run dev`)
- [ ] تأكد من تشغيل Laravel (`php artisan serve`)
- [ ] امسح ذاكرة التخزين المؤقت (Ctrl+Shift+Delete)
- [ ] أعد تحميل الصفحة (Ctrl+Shift+R)

### اختبار الزر
- [ ] الزر موجود في الهيدر
- [ ] الزر يحتوي على أيقونة قمر/شمس
- [ ] الزر قابل للنقر
- [ ] الزر يستجيب عند الضغط

### اختبار التبديل
- [ ] الألوان تتغير عند الضغط
- [ ] التغيير يحدث فوراً
- [ ] جميع العناصر تتغير
- [ ] لا توجد عناصر متبقية بالوضع السابق

### اختبار الحفظ
- [ ] اختر الوضع المظلم
- [ ] افتح أدوات المطور (F12)
- [ ] انتقل إلى Application > Local Storage
- [ ] ابحث عن `hospital-theme-mode`
- [ ] تحقق من أن القيمة هي `dark`
- [ ] أغلق المتصفح تماماً
- [ ] أعد فتح التطبيق
- [ ] تحقق من أن الوضع المظلم محفوظ

### اختبار الصفحات
- [ ] Dashboard
- [ ] Patients
- [ ] Doctors
- [ ] Appointments
- [ ] جميع الصفحات الأخرى

## 🔧 إصلاح سريع

إذا لم تنجح الحلول أعلاه، جرب هذا:

### 1. امسح كل شيء
```javascript
// امسح localStorage
localStorage.clear()

// أعد تحميل الصفحة
window.location.reload()
```

### 2. أعد بناء الأصول
```bash
# في المجلد الرئيسي للمشروع
npm run build

# ثم أعد تحميل الصفحة
```

### 3. تحقق من الملفات
```bash
# تأكد من وجود الملفات
ls resources/js/theme.js
ls resources/css/app.css
```

## 📊 معلومات التصحيح

### معرّفات العناصر المهمة
```
الزر: #theme-toggle-btn
الفئة المظلمة: dark
مفتاح التخزين: hospital-theme-mode
```

### الأحداث المخصصة
```javascript
// استمع لتغييرات الوضع
window.addEventListener('themechange', (e) => {
    console.log('Theme changed to:', e.detail.isDark ? 'dark' : 'light');
});
```

## 💡 نصائح مفيدة

1. **استخدم Console بكثرة** - اطبع الرسائل لتتبع التنفيذ
2. **استخدم DevTools** - استخدم Elements لفحص الفئات
3. **اختبر على متصفحات مختلفة** - قد تكون هناك مشاكل متصفح محددة
4. **امسح الـ Cache** - غالباً ما يحل المشاكل

## 📞 الدعم

إذا استمرت المشاكل:
1. تحقق من وحدة التحكم للأخطاء
2. تحقق من ملف theme.js
3. تحقق من ملف app.css
4. تحقق من ملف app.blade.php

---

**ملاحظة**: جميع أوامر التصحيح متاحة في Console عند تحميل الصفحة.

